package adapter

import adapter.model.Point

internal class TextManipulator(private val manipulatedShape: Shape) : Manipulator {
    override fun moveShape(destination: Point) {
        // TODO("not implemented")
    }
}