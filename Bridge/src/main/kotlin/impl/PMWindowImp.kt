package impl

import model.Point

internal class PMWindowImp : WindowImp {
    override fun deviceRect(upperLeft: Point, bottomRight: Point) {
        super.deviceRect(upperLeft, bottomRight)
        // specific implementation
    }
}